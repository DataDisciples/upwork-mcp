# Port the Go bridge listens on (Railway sets PORT automatically, but
# the bridge defaults to 8080 — leave this unset unless you need to override)
# MCP_PORT=8080

# Optional: set timezone for message timestamps
# TZ=America/New_York
